不支持随机访问：与数组和vector相比，list不支持随机访问，即不能使用下标操作符[]来访问元素。因此，访问list中的元素通常需要使用迭代器。

插入和删除操作：list的插入和删除操作不会导致迭代器失效，这是因为list内部使用了链表结构。所以，在对list进行插入和删除操作时，不需要像处理vector那样担心迭代器失效的问题

排序操作：list不支持使用std::sort()函数进行排序，但是可以使用其成员函数list::sort()进行排序。