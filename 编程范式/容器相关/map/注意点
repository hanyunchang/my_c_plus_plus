自动排序：Map中的元素会根据键值自动排序，排序的规则由比较函数决定，默认是std::less<Key>，即小于运算符。如果需要其他排序方式，可以在创建map时指定比较函数。

查找元素：使用map的find函数可以查找元素，如果找到了，会返回一个iterator指向该元素，如果没找到，会返回map::end。

键值类型：键值类型需要支持比较运算符，如果键值类型是自定义类型，可能需要重载比较运算符。

map不支持随机访问：map的迭代器是双向迭代器，不能进行加减运算和随机访问。